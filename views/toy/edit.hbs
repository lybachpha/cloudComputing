
<div class="d-flex justify-content-center align-items-center" style="min-height: 100vh; ">
   <div class="col-md-8 ">
   <form action="/toy/edit/{{toy._id}}" method="post" style="margin-top: -150px;">
    <h1 class="text-center">Edit toy</h1>
            <div class="form-group row">
                <label for="name" class="col-sm-2 col-form-label">Toy name:</label>
                <div class="col-sm-10">
                    <input type="text" class="form-control" name="name" placeholder="Toy name" required value="{{toy.name}}">
                </div>
            </div>
            <div class="form-group row">
                <label for="image" class="col-sm-2 col-form-label">Toy image:</label>
                <div class="col-sm-10">
                    <input type="url" class="form-control" name="image" placeholder="Toy image URL" required value="{{toy.image}}">
                </div>
            </div>
            <div class="form-group row">
                <label for="category" class="col-sm-2 col-form-label">Toy category:</label>
                <div class="col-sm-10">
                    <input type="text" class="form-control" name="category" placeholder="Toy category" required value="{{toy.category}}">
                </div>
            </div>
            <div class="form-group row">
                <label for="price" class="col-sm-2 col-form-label">Toy price:</label>
                <div class="col-sm-10">
                   
                    <input type="number" class="form-control " name="price" placeholder="Toy price" required value="{{toy.price}}">
                </div>
            </div>
            <div class="form-group row">
              <label for="brand" class="col-sm-2 col-form-label">Brand:</label>
                <div class="col-sm-10">
                <select class="form-control mb-3" name="brand" id="brand" placeholder="Toy brand">
                 {{#each brands}}
                 <option 
                     value="{{this._id}}" >{{this.name}}
                 </option>
                {{/each}}
                </select>
                 </div>
            </div>

            <div class="text-center">
                   <input type="submit" value="Add" class="btn btn-primary">
            </div>


       </form>
   
</div>
</div>
<style>
    
     label {
        color: rgb(128, 128, 192); /* Thay 'red' bằng mã màu hoặc tên màu bạn muốn sử dụng */
    }
    h1{
      color: rgb(139, 214, 27);
    }
</style>
